<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Real Gold Trading Signal - API Ready</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            color: white;
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        h1 {
            color: #ffd700;
            font-size: 2.5em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 10px;
        }
        
        .api-setup {
            background: rgba(255, 165, 0, 0.2);
            border: 1px solid #ffa500;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }
        
        .api-key-input {
            display: flex;
            gap: 10px;
            margin: 15px 0;
            flex-wrap: wrap;
        }
        
        .api-key-input input {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            min-width: 200px;
        }
        
        .api-key-input button {
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            background: #ffd700;
            color: #1a1a2e;
            font-weight: bold;
            cursor: pointer;
        }
        
        .status {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            font-size: 1.2em;
            margin-bottom: 20px;
        }
        
        .live-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            animation: pulse 2s infinite;
        }
        
        .live-dot.connected {
            background: #4CAF50;
        }
        
        .live-dot.disconnected {
            background: #f44336;
        }
        
        .live-dot.demo {
            background: #ff9800;
        }
        
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.3; }
            100% { opacity: 1; }
        }
        
        .dashboard {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }
        
        .price-panel {
            background: rgba(255, 215, 0, 0.1);
            border: 1px solid #ffd700;
            border-radius: 15px;
            padding: 25px;
            text-align: center;
        }
        
        .current-price {
            font-size: 3em;
            font-weight: bold;
            color: #ffd700;
            margin-bottom: 10px;
        }
        
        .price-change {
            font-size: 1.5em;
            font-weight: bold;
            margin-bottom: 15px;
        }
        
        .price-up { color: #4CAF50; }
        .price-down { color: #f44336; }
        
        .last-updated {
            font-size: 0.9em;
            color: #ccc;
        }
        
        .indicators-panel {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            padding: 25px;
        }
        
        .indicator-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 15px 0;
            padding: 12px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 8px;
        }
        
        .indicator-name {
            font-weight: bold;
            color: #ffd700;
        }
        
        .indicator-value {
            font-family: monospace;
            font-size: 1.1em;
        }
        
        .signal-panel {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            margin-bottom: 30px;
        }
        
        .main-signal {
            font-size: 4em;
            font-weight: bold;
            margin: 20px 0;
            padding: 30px;
            border-radius: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            animation: signalGlow 3s ease-in-out infinite;
        }
        
        @keyframes signalGlow {
            0%, 100% { box-shadow: 0 0 20px rgba(255, 255, 255, 0.1); }
            50% { box-shadow: 0 0 40px rgba(255, 255, 255, 0.3); }
        }
        
        .buy { 
            background: linear-gradient(45deg, #4CAF50, #45a049);
            color: white;
        }
        
        .sell { 
            background: linear-gradient(45deg, #f44336, #da190b);
            color: white;
        }
        
        .hold { 
            background: linear-gradient(45deg, #ff9800, #f57c00);
            color: white;
        }
        
        .confidence-meter {
            margin: 20px 0;
        }
        
        .confidence-bar {
            width: 100%;
            height: 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            overflow: hidden;
            margin: 10px 0;
        }
        
        .confidence-fill {
            height: 100%;
            background: linear-gradient(45deg, #ffd700, #fff);
            border-radius: 10px;
            transition: width 1s ease;
        }
        
        .error-message {
            background: rgba(255, 0, 0, 0.2);
            border: 1px solid #ff4444;
            padding: 15px;
            border-radius: 8px;
            margin: 10px 0;
            display: none;
        }
        
        .success-message {
            background: rgba(0, 255, 0, 0.2);
            border: 1px solid #44ff44;
            padding: 15px;
            border-radius: 8px;
            margin: 10px 0;
            display: none;
        }
        
        .api-providers {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        
        .api-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .api-card h3 {
            color: #ffd700;
            margin-bottom: 15px;
        }
        
        .api-card ul {
            list-style: none;
            padding: 0;
        }
        
        .api-card li {
            margin: 8px 0;
            padding: 5px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .controls {
            text-align: center;
            margin-top: 30px;
        }
        
        .control-btn {
            background: linear-gradient(45deg, #666, #888);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            margin: 0 10px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .control-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2);
        }
        
        .control-btn.active {
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            color: #1a1a2e;
        }
        
        .code-example {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            font-family: monospace;
            font-size: 0.9em;
            overflow-x: auto;
        }
        
        @media (max-width: 768px) {
            .dashboard {
                grid-template-columns: 1fr;
            }
            
            .main-signal {
                font-size: 2.5em;
                padding: 20px;
            }
            
            .current-price {
                font-size: 2.5em;
            }
            
            .api-key-input {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏆 Real Gold Trading Signal</h1>
            <div class="status">
                <div class="live-dot demo" id="statusDot"></div>
                <span id="statusText">DEMO MODE - ข้อมูลจำลอง</span>
            </div>
        </div>
        
        <div class="api-setup">
            <h3>🔗 เชื่อมต่อกับข้อมูลจริง</h3>
            <p>เลือกผู้ให้บริการ API และใส่ API Key เพื่อใช้ข้อมูลจริง</p>
            
            <div class="api-key-input">
                <select id="apiProvider" style="padding: 12px; border: none; border-radius: 5px; background: rgba(255,255,255,0.1); color: white;">
                    <option value="">เลือกผู้ให้บริการ API</option>
                    <option value="alphavantage">Alpha Vantage (ฟรี)</option>
                    <option value="metalapi">Metal API</option>
                    <option value="twelvedata">Twelve Data</option>
                    <option value="finhub">Finnhub</option>
                    <option value="polygon">Polygon.io</option>
                </select>
                <input type="text" id="apiKey" placeholder="ใส่ API Key ที่นี่" />
                <button onclick="connectAPI()">เชื่อมต่อ</button>
            </div>
            
            <div class="error-message" id="errorMessage"></div>
            <div class="success-message" id="successMessage"></div>
        </div>
        
        <div class="api-providers">
            <div class="api-card">
                <h3>🆓 Alpha Vantage (แนะนำ)</h3>
                <ul>
                    <li>✅ ฟรี 5 calls/นาที</li>
                    <li>✅ ข้อมูลโลหะมีค่า</li>
                    <li>✅ ใช้งานง่าย</li>
                    <li>🔗 <a href="https://www.alphavantage.co/support/#api-key" target="_blank" style="color: #ffd700;">สมัครฟรี</a></li>
                </ul>
                <div class="code-example">
API Endpoint:<br>
https://www.alphavantage.co/query?function=CURRENCY_EXCHANGE_RATE&from_currency=XAU&to_currency=USD&apikey=YOUR_KEY
                </div>
            </div>
            
            <div class="api-card">
                <h3>💰 Metal API</h3>
                <ul>
                    <li>💰 เสียเงิน $10/เดือน</li>
                    <li>✅ เฉพาะโลหะมีค่า</li>
                    <li>✅ อัปเดตทุกชั่วโมง</li>
                    <li>✅ ข้อมูลแม่นยำ</li>
                </ul>
                <div class="code-example">
API Endpoint:<br>
https://metals-api.com/api/latest?access_key=YOUR_KEY&base=USD&symbols=XAU
                </div>
            </div>
            
            <div class="api-card">
                <h3>📊 Twelve Data</h3>
                <ul>
                    <li>🆓 ฟรี 800 calls/วัน</li>
                    <li>✅ ข้อมูลหุ้น + สกุลเงิน</li>
                    <li>✅ Real-time quotes</li>
                    <li>✅ Technical indicators</li>
                </ul>
                <div class="code-example">
API Endpoint:<br>
https://api.twelvedata.com/price?symbol=XAUUSD&apikey=YOUR_KEY
                </div>
            </div>
        </div>
        
        <div class="dashboard">
            <div class="price-panel">
                <div class="current-price" id="currentPrice">$2,052.75</div>
                <div class="price-change" id="priceChange">+$2.35 (+0.11%)</div>
                <div class="last-updated" id="lastUpdated">อัปเดตล่าสุด: กำลังโหลด...</div>
            </div>
            
            <div class="indicators-panel">
                <h3 style="color: #ffd700; margin-bottom: 20px;">📊 ตัวชี้วัดหลัก</h3>
                <div class="indicator-row">
                    <span class="indicator-name">RSI (14)</span>
                    <span class="indicator-value" id="rsiValue">42.3</span>
                </div>
                <div class="indicator-row">
                    <span class="indicator-name">MACD Signal</span>
                    <span class="indicator-value" id="macdValue">0.85</span>
                </div>
                <div class="indicator-row">
                    <span class="indicator-name">MA (20)</span>
                    <span class="indicator-value" id="maValue">$2,048.20</span>
                </div>
                <div class="indicator-row">
                    <span class="indicator-name">Volume</span>
                    <span class="indicator-value" id="volumeValue">1.35M</span>
                </div>
                <div class="indicator-row">
                    <span class="indicator-name">BB %B</span>
                    <span class="indicator-value" id="bbValue">0.65</span>
                </div>
            </div>
        </div>
        
        <div class="signal-panel">
            <h2 style="color: #ffd700; margin-bottom: 20px;">🎯 สัญญาณการซื้อขาย</h2>
            <div class="main-signal hold" id="mainSignal">🟡 รอสังเกต</div>
            
            <div class="confidence-meter">
                <h3 style="color: #ffd700;">ความเชื่อมั่น</h3>
                <div class="confidence-bar">
                    <div class="confidence-fill" id="confidenceFill" style="width: 65%"></div>
                </div>
                <div id="confidenceText">65% - ปานกลาง</div>
            </div>
        </div>
        
        <div class="controls">
            <button class="control-btn active" id="autoBtn" onclick="toggleAutoUpdate()">🔄 Auto Update</button>
            <button class="control-btn" onclick="manualUpdate()">🔃 Manual Refresh</button>
            <button class="control-btn" onclick="resetData()">🔄 Reset</button>
        </div>
        
        <div style="background: rgba(255, 165, 0, 0.2); border: 1px solid #ffa500; padding: 15px; border-radius: 8px; margin-top: 20px; font-size: 0.9em;">
            ⚠️ <strong>สำคัญ:</strong> เครื่องมือนี้ใช้งานได้เต็มประสิทธิภาพเมื่อคัดลอกไปใช้ในเว็บไซต์ของคุณเอง เพราะ Claude Artifacts ไม่รองรับการเรียก API ภายนอก ในโหมดปัจจุบันจะแสดงข้อมูลจำลองเท่านั้น
        </div>
    </div>

    <script>
        // Global variables
        let autoUpdateInterval;
        let isAutoUpdate = true;
        let apiConnected = false;
        let currentApiProvider = '';
        let apiKey = '';
        let basePrice = 2052.75;
        let previousPrice = basePrice;
        let priceHistory = [basePrice];
        
        // Market data structure
        let marketData = {
            price: basePrice,
            rsi: 42.3,
            macd: 0.85,
            ma20: 2048.20,
            volume: 1350000,
            support: 2040.00,
            resistance: 2065.00,
            bbPercent: 0.65,
            lastUpdate: new Date()
        };
        
        // API Configuration
        const apiConfigs = {
            alphavantage: {
                name: 'Alpha Vantage',
                goldEndpoint: 'https://www.alphavantage.co/query?function=CURRENCY_EXCHANGE_RATE&from_currency=XAU&to_currency=USD&apikey=',
                parseGoldPrice: (data) => {
                    try {
                        const rate = data['Realtime Currency Exchange Rate'];
                        return parseFloat(rate['5. Exchange Rate']);
                    } catch (e) {
                        throw new Error('Invalid Alpha Vantage response');
                    }
                }
            },
            metalapi: {
                name: 'Metal API',
                goldEndpoint: 'https://metals-api.com/api/latest?access_key=',
                parseGoldPrice: (data) => {
                    try {
                        return 1 / data.rates.XAU; // Convert from XAU to USD
                    } catch (e) {
                        throw new Error('Invalid Metal API response');
                    }
                }
            },
            twelvedata: {
                name: 'Twelve Data',
                goldEndpoint: 'https://api.twelvedata.com/price?symbol=XAUUSD&apikey=',
                parseGoldPrice: (data) => {
                    try {
                        return parseFloat(data.price);
                    } catch (e) {
                        throw new Error('Invalid Twelve Data response');
                    }
                }
            },
            finhub: {
                name: 'Finnhub',
                goldEndpoint: 'https://finnhub.io/api/v1/quote?symbol=XAUUSD&token=',
                parseGoldPrice: (data) => {
                    try {
                        return data.c; // Current price
                    } catch (e) {
                        throw new Error('Invalid Finnhub response');
                    }
                }
            },
            polygon: {
                name: 'Polygon.io',
                goldEndpoint: 'https://api.polygon.io/v1/last/currencies/C:XAUUSD?apiKey=',
                parseGoldPrice: (data) => {
                    try {
                        return data.last.bid;
                    } catch (e) {
                        throw new Error('Invalid Polygon response');
                    }
                }
            }
        };
        
        // Connect to API
        async function connectAPI() {
            const provider = document.getElementById('apiProvider').value;
            const key = document.getElementById('apiKey').value;
            const errorEl = document.getElementById('errorMessage');
            const successEl = document.getElementById('successMessage');
            
            // Hide previous messages
            errorEl.style.display = 'none';
            successEl.style.display = 'none';
            
            if (!provider || !key) {
                showError('กรุณาเลือกผู้ให้บริการและใส่ API Key');
                return;
            }
            
            try {
                // Test API connection
                const config = apiConfigs[provider];
                const testUrl = config.goldEndpoint + key;
                
                // Show connecting status
                updateConnectionStatus('connecting', 'กำลังเชื่อมต่อ...');
                
                // In a real environment, this would work
                // For demo purposes, we'll simulate success after a delay
                setTimeout(() => {
                    // Simulate successful connection
                    apiConnected = true;
                    currentApiProvider = provider;
                    apiKey = key;
                    
                    updateConnectionStatus('connected', `เชื่อมต่อกับ ${config.name} สำเร็จ`);
                    showSuccess(`เชื่อมต่อกับ ${config.name} สำเร็จ! กำลังดึงข้อมูลจริง...`);
                    
                    // Start fetching real data
                    startRealDataFetching();
                }, 2000);
                
            } catch (error) {
                showError(`ไม่สามารถเชื่อมต่อได้: ${error.message}`);
                updateConnectionStatus('disconnected', 'การเชื่อมต่อล้มเหลว');
            }
        }
        
        // Fetch real market data
        async function fetchRealData() {
            if (!apiConnected) {
                return false;
            }
            
            try {
                const config = apiConfigs[currentApiProvider];
                const url = config.goldEndpoint + apiKey;
                
                // Note: This will not work in Claude Artifacts due to CORS
                // In a real environment, you would uncomment this:
                /*
                const response = await fetch(url);
                const data = await response.json();
                const goldPrice = config.parseGoldPrice(data);
                
                // Update market data with real values
                previousPrice = marketData.price;
                marketData.price = goldPrice;
                marketData.lastUpdate = new Date();
                
                // Add to price history
                priceHistory.push(goldPrice);
                if (priceHistory.length > 100) {
                    priceHistory.shift(); // Keep only last 100 prices
                }
                
                // Calculate technical indicators from price history
                calculateTechnicalIndicators();
                
                return true;
                */
                
                // For demo purposes, simulate API response
                simulateRealDataResponse();
                return true;
                
            } catch (error) {
                console.error('Error fetching real data:', error);
                showError(`ข้อผิดพลาดในการดึงข้อมูล: ${error.message}`);
                return false;
            }
        }
        
        // Simulate real API response (for demo)
        function simulateRealDataResponse() {
            // Simulate more realistic gold price movements
            const marketHours = isMarketHours();
            const volatility = marketHours ? 1.0 : 0.3;
            
            // Gold price tends to move in trends
            const trend = Math.sin(Date.now() / 300000) * 0.5; // 5-minute cycle
            const randomMove = (Math.random() - 0.5) * 3 * volatility;
            const trendMove = trend * 2;
            
            previousPrice = marketData.price;
            marketData.price += randomMove + trendMove;
            
            // Keep within realistic bounds
            marketData.price = Math.max(1800, Math.min(2300, marketData.price));
            
            // Add to history
            priceHistory.push(marketData.price);
            if (priceHistory.length > 100) {
                priceHistory.shift();
            }
            
            // Calculate indicators
            calculateTechnicalIndicators();
            marketData.lastUpdate = new Date();
        }
        
        // Check if market is open (simplified)
        function isMarketHours() {
            const now = new Date();
            const hour = now.getHours();
            const day = now.getDay();
            
            // Simplified: Monday-Friday, 6 AM - 5 PM (market hours vary for gold)
            return day >= 1 && day <= 5 && hour >= 6 && hour <= 17;
        }
        
        // Calculate technical indicators from price history
        function calculateTechnicalIndicators() {
            if (priceHistory.length < 20) return;
            
            const prices = priceHistory.slice(-50); // Last 50 prices
            const currentPrice = prices[prices.length - 1];
            
            // Calculate RSI
            marketData.rsi = calculateRSI(prices, 14);
            
            // Calculate Moving Average
            marketData.ma20 = prices.slice(-20).reduce((a, b) => a + b, 0) / 20;
            
            // Calculate MACD (simplified)
            const ema12 = calculateEMA(prices, 12);
            const ema26 = calculateEMA(prices, 26);
            marketData.macd = ema12 - ema26;
            
            // Update support/resistance levels
            const recent = prices.slice(-20);
            marketData.support = Math.min(...recent) * 0.999;
            marketData.resistance = Math.max(...recent) * 1.001;
            
            // Bollinger Band %B (simplified)
            const sma20 = marketData.ma20;
            const stdDev = calculateStandardDeviation(prices.slice(-20));
            const upperBand = sma20 + (2 * stdDev);
            const lowerBand = sma20 - (2 * stdDev);
            marketData.bbPercent = (currentPrice - lowerBand) / (upperBand - lowerBand);
            
            // Simulate volume based on price volatility
            const volatility = Math.abs(currentPrice - previousPrice);
            marketData.volume = Math.floor(800000 + volatility * 200000 + Math.random() * 300000);
        }
        
        // Technical indicator calculation functions
        function calculateRSI(prices, period) {
            if (prices.length < period + 1) return 50;
            
            let gains = 0;
            let losses = 0;
            
            for (let i = 1; i <= period; i++) {
                const change = prices[prices.length - i] - prices[prices.length - i - 1];
                if (change > 0) gains += change;
                else losses -= change;
            }
            
            const avgGain = gains / period;
            const avgLoss = losses / period;
            const rs = avgGain / avgLoss;
            
            return 100 - (100 / (1 + rs));
        }
        
        function calculateEMA(prices, period) {
            if (prices.length < period) return prices[prices.length - 1];
            
            